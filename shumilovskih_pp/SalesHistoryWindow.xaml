<Window x:Class="shumilovskih_pp.SalesHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="История продаж - shumilovskih_pp" 
        Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="История продаж партнера" FontSize="20" FontWeight="Bold" 
                   Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,10"/>

        <Border Grid.Row="1" Background="#E8F5E9" Padding="10" Margin="0,0,0,10" 
                BorderBrush="{StaticResource SuccessBrush}" BorderThickness="1" CornerRadius="4">
            <StackPanel>
                <TextBlock x:Name="PartnerNameTextBlock" Text="Партнер: " FontWeight="Bold" FontSize="14"
                           Foreground="{StaticResource PrimaryBrush}"/>
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <TextBlock Text="Общая сумма продаж: " FontWeight="Bold"/>
                    <TextBlock x:Name="TotalSalesTextBlock" Text="0 ₽" FontWeight="Bold" 
                               Foreground="{StaticResource SuccessBrush}" Margin="5,0,20,0"/>
                    <TextBlock Text="Скидка: " FontWeight="Bold"/>
                    <TextBlock x:Name="DiscountTextBlock" Text="0%" FontWeight="Bold" 
                               Foreground="{StaticResource DangerBrush}" Margin="5,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" x:Name="SalesDataGrid" 
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" SelectionUnit="FullRow"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  SelectionChanged="SalesDataGrid_SelectionChanged"
                  BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding SaleId}" Width="50"/>
                <DataGridTextColumn Header="Продукция" Binding="{Binding Product.ProductName}" Width="150"/>
                <DataGridTextColumn Header="Артикул" Binding="{Binding Product.Article}" Width="100"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding TotalAmount, StringFormat={}{0:N2} ₽}" Width="120"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding SaleDate, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="Добавить" Width="130" Click="AddSale_Click" 
                        Style="{StaticResource NeutralButtonStyle}"/>
                <Button Content="Изменить" Width="100" Click="EditSale_Click" 
                        Style="{StaticResource NeutralButtonStyle}"/>
                <Button Content="Удалить" Width="100" Click="DeleteSale_Click" 
                       Style="{StaticResource NeutralButtonStyle}"/>
            </StackPanel>


            <Button Grid.Column="1" Content="Закрыть" Width="100" Click="Close_Click" 
                    Style="{StaticResource NeutralButtonStyle}"/>
        </Grid>
    </Grid>
</Window>